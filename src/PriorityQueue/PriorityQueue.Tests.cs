namespace PriorityQueue
{
    using System.Collections.Generic;
    using NUnit.Framework;

    public class PriorityQueueTests
    {
        //private PriorityQueue<int> priorityQueue;

        //[SetUp]
        //public void InitializeQueue()
        //    => this.priorityQueue = new PriorityQueue<int>();

        //[Test]
        //public void TestDequeueSingleElement()
        //{
        //    this.priorityQueue.Add(13);
        //    Assert.AreEqual(13, this.priorityQueue.Dequeue());
        //}

        //[Test]
        //public void TestDequeueMultipleElements()
        //{
        //    var elements = new List<int> { 15, 25, 6, 9, 5, 8, 17, 16 };

        //    foreach (var element in elements)
        //    {
        //        this.priorityQueue.Add(element);
        //    }

        //    var index = 0;
        //    var expected = new[] { 25, 17, 16, 15, 9, 8, 6, 5 };

        //    Assert.AreEqual(expected.Length, this.priorityQueue.Size);

        //    while (this.priorityQueue.Size != 0)
        //    {
        //        Assert.AreEqual(expected[index++], this.priorityQueue.Dequeue());
        //    }
        //}
    }
}